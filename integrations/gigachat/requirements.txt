# Complete HTTP runtime graph for the secret-bearing GigaChat API step.
# Keep every runtime dependency exact; updates must pass the scoped clean-install CI.
certifi==2026.7.22
charset-normalizer==3.4.9
idna==3.18
requests==2.34.2
urllib3==2.7.0
